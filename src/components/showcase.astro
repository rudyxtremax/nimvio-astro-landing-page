---
import ShowcaseList from "./showcase-list.astro";
const { data } = Astro.props;
---

<Fragment>
  {
    data.contentList.ContentIDs.map((id: string) => (
      <ShowcaseList contentId={id} />
    ))
  }
</Fragment>
