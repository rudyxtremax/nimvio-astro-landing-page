---
import CtaButton from "~/components/cta-button.astro";

const { data } = Astro.props;
---

<div class="grid grid-cols-1 gap-2 sm:grid-cols-2">
  {data.contentList.ContentIDs.map((id: string) => <CtaButton contentId={id}/>)}
</div>
